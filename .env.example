# Do NOT commit secrets. Use this file as a template and add real values in your environment or CI secrets.
# Supervisor token for Home Assistant internal API
SUPERVISOR_TOKEN=

# Webhook secret (HMAC or JWT secret). Store this in GitHub Secrets or Home Assistant addon options.
WEBHOOK_SECRET=
